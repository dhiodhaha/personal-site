import { categoryType } from './category';
import { postType } from './post';
import { profileType } from './profile';
import { workType } from './work';

export const schemaTypes = [workType, postType, categoryType, profileType];
